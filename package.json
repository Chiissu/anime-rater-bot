{
  "name": "animerater",
  "module": "src/index.ts",
  "version": "1.0.0",
  "scripts": {
    "start": "bun run src/index.ts",
    "debug": "bun --inspect=localhost src/index.ts",
    "docker": "docker build --pull -t anime-rater . && docker run -d anime-rater"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "type": "module",
  "dependencies": {
    "discord.js": "^14.14.1"
  }
}
